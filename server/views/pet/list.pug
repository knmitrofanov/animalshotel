extends ../shared/layout

block main-content
    | Total count of pets:   
    span.float-xs-right= model.length

    ul.list-group
        each pet in model
            li
                br
                a.btn.btn-success(href="/pets/" + pet.id)= pet.name