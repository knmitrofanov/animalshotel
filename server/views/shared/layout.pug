doctype
html
head
	title Title
body
	header.nav
	.container
		.navbar-header
			a.navbar-brand(href="/") Animal Hotel
		.navbar-collapse.collapse
			ul.nav.navbar-nav
				li
					a(href="/") Home
				if (currentUser)
					li
						a(href="/auth/profile") #{currentUser.username}
					li
						a(href="/auth/logout") Logout
				if (!currentUser)
					li
						a(href="/auth/register") Register
					li
						a(href="/auth/login") Login

	.container
		if (errorMessage)
			.alert.alert-danger #{errorMessage}

		block main-content

	footer
		p footeeeer

	include scripts